use crate::solutions::level::Level;

pub struct Level3;

impl Level for Level3 {
    fn level(&self) -> usize {
        3
    }

    fn solve(&self, input: &str) -> Option<String> {
        None
    }
}
